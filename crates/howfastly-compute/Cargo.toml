[package]
name    = "howfastly-compute"
edition = "2024"
version = "2026.708.0"

# the bin only builds for wasm32-wasip1, keep it out of host test builds
[[bin]]
name = "howfastly-compute"
path = "src/main.rs"
test = false

[dependencies]
common.workspace      = true
fastly.workspace      = true
include_dir.workspace = true
serde_json.workspace  = true
